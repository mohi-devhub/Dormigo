spring.application.name=dormigo-backend
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/dormigo_db
spring.datasource.username=postgres
spring.datasource.password=kunfu
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway Configuration (Database Migrations)
spring.flyway.enabled=false
#spring.flyway.locations=classpath:db/migration
#spring.flyway.baseline-on-migrate=true

# JWT Configuration secret is the text
jwt.secret=dormigo-super-secret-key-for-jwt-token-signing-must-be-at-least-256-bits-long-for-hs256-algorithm
jwt.expiration=86400000

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.example.demo=DEBUG

spring.rabbitmq.host=disabled
spring.rabbitmq.port=0


#Image Properties

file.directory=uploads/products
file.max-size=5242880
file.allowed-extensions=png, jpeg, jpg, gif, webp

#Multi part Configuration

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# ... existing properties ...

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001
cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true
cors.max-age=3600

#Image Service
cloudinary.cloud-name=dywfubilx
cloudinary.api-key=253895954591916
cloudinary.api-secret=zCDL0ZadBbtKaOBEeDXxBgfayyo
cloudinary.folder=dormigo-products

# ========== EMAIL CONFIGURATION ==========

# Gmail SMTP Configuration (for development)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dormigoservice@gmail.com
spring.mail.password=zwib eliz eeoa fqwr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email From Address
app.email.from=${spring.mail.username}
app.email.from-name=Dormigo Services

# Email Templates
spring.freemarker.template-loader-path=classpath:/email-templates/
spring.freemarker.suffix=.ftl


#Redis Config

spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.timeout=60000
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0

#Cache Config
spring.cache.type=redis 
spring.cache.redis.time-to-live=600000

#Key Prefix

spring.cache.redis.key-prefix=dormigo::
spring.cache.redis.use-key-prefix=true